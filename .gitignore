# UFLD lane model (117 MB) is bundled locally at build time;
# the canonical copy lives as a GitHub release asset (see ModelDownloader).
app/src/main/assets/ufld_tusimple_float16.tflite

# Build outputs
.gradle/
app/build/
build/
local.properties

# Stray LaTeX scratch files (see feature/hud-alert-feedback: t3_*.pdf etc. in repo root)
*.aux
*.log
*.out
*.toc
*.pdf
